# git-svn-rs Implementation Progress - 2026-06-15

This document records the current execution state of the plans in `.plans/`.

## Current Branch

- Branch: `codex-execute-git-svn-rs-plans`
- Base: `master` at `1284668 Add planning documents`

## Latest Update

Committed the previously pending Phase 4/5/8 follow-up batch:

- `97d4352 feat: extend svn fixture import coverage`
- `d898981 feat: preserve svn executable mode in cli import`
- `a3712d2 feat: preserve svn special links in cli import`
- `cb30882 test: compare file modes in golden harness`

New completed and committed after that batch:

- Added `svn:executable` coverage to the real `file://` SVN fixture.
- Taught the SVN CLI backend to read `svn:executable` with `svn propget`.
- Restored real import mode mapping so executable SVN files become Git mode `100755`.
- Added `svn:special` coverage to the real `file://` SVN fixture.
- Taught the SVN CLI backend to read `svn:special` with `svn propget`.
- Restored real import mode/content mapping so SVN special links become Git mode `120000` with the `link ` prefix stripped.
- Extended the golden compatibility harness to capture and compare Git tree file modes.
- Extended the golden trunk fixture with executable and `svn:special` paths so mode comparison covers `100755` and `120000`.
- Extended the golden trunk fixture with a deleted path edge case.
- Fixed SVN CLI historical content/property/list lookups to use peg revisions, so paths deleted in HEAD can still be read at earlier revisions.
- Tightened Perl git-svn detection so `git-svn-rs` shim output is treated as missing Perl git-svn instead of a valid golden comparison backend.

These file mode follow-ups have been verified locally and committed.

Newly completed locally:

- Phase 5 real local `file://` clone/fetch imports now apply configured `--ignore-paths` and `--include-paths` filters before mapping SVN paths into Git.
- Phase 7 real local `file://` SVN dcommit now handles Git type-change (`T`) diffs and removes `svn:special` when a symlink becomes a regular file.
- Phase 7 real local `file://` SVN dcommit now removes `svn:executable` when a Git file's executable mode is cleared.
- Phase 8 golden compatibility artifacts now capture and compare preserved empty-directory placeholder paths.
- Phase 8 standard trunk golden comparison now runs Perl and Rust clones with `--preserve-empty-dirs --placeholder-filename .gitkeep` for the supported subset.
- Phase 5 `--preserve-empty-dirs` and `--placeholder-filename` are now parsed, persisted in SVN remote config, and honored by real local `file://` clone/fetch imports.
- Phase 5 real local `file://` clone/fetch now materializes empty SVN directories as configurable placeholder files when requested.
- Phase 7 real local `file://` SVN dcommit now has end-to-end coverage for `svn:special` symlink write-back.
- Phase 7 real local `file://` SVN dcommit now writes `svn:executable` for executable Git files, with end-to-end SVN property coverage.
- Phase 7 real local `file://` SVN dcommit coverage now verifies file add and delete write-back.
- Phase 7 `dcommit` default rebase behavior is covered for real local `file://` SVN write-back; after dcommit, `HEAD` rebases to the tracked SVN ref.
- Phase 7 `dcommit --no-rebase` now writes a linear local commit back to real local `file://` SVN repositories through the SVN CLI path.
- Incremental fetch/import now anchors the first newly imported commit to the existing tracked ref commit, allowing post-dcommit fetch to fast-forward refs.
- Per-mapping import now skips revisions already present in that mapping's rev_map, avoiding duplicate rev_map appends during mixed trunk/branch/tag fetches.
- Phase 7 `dcommit --dry-run --mergeinfo VALUE` now accepts explicit mergeinfo and prints the v1 scope message while keeping non-dry-run mergeinfo write-back guarded.
- Phase 8 normalized `find-rev <commit>` artifact capture and comparison in the golden compatibility harness.
- Phase 8 normalized `find-rev --before/--after rN` artifact capture and comparison in the golden compatibility harness.
- Phase 8 normalized `log --revision rN --oneline` artifact capture and comparison in the golden compatibility harness.
- Phase 8 normalized full `info` artifact capture and comparison in the golden compatibility harness.
- Phase 6 `info --url` now reports the tracked SVN URL including the fetch mapping path for standard-layout style configs.
- Phase 8 normalized `info --url` artifact capture and comparison in the golden compatibility harness.
- Phase 8 normalized `log --oneline` artifact capture and comparison in the golden compatibility harness.
- Phase 8 normalized `find-rev rN` artifact capture and comparison in the golden compatibility harness, avoiding direct Git object ID comparisons.
- Phase 6 `reset`
- Phase 6 `rebase`
- Phase 7 `dcommit --dry-run`
- Phase 7 `--mergeinfo` v1 scope message
- Phase 8 golden compatibility harness skeleton
- Phase 4 diagnostics/libsvn feature shell
- Phase 5 minimal real `file://` SVN CLI fetch/clone replay
- Phase 5 executable file mode preservation for SVN CLI replay
- Phase 5 symlink mode/content preservation for SVN CLI replay
- Phase 5 deleted path handling after branch/tag copies through SVN CLI replay
- Phase 8 golden file mode comparison for executable and symlink paths
- Phase 8 golden delete edge scenario
- Phase 7 mock-only `dcommit` write-back

Latest verification:

- `cargo fmt --check`
- `cargo test -p git-svn-rs --test clone_fetch_real_svn -- --nocapture`
- `cargo test -p git-svn-rs-core --test import_mock`
- `cargo test -p git-svn-rs-core --test compat_golden -- --nocapture`
- `cargo test -p git-svn-rs-core --test svn_fixture -- --nocapture`
- `cargo clippy --all-targets --all-features -- -D warnings`

## Committed Completed Work

### Phase 1: Foundation CLI Workspace

Status: completed and committed.

Commit:

- `7f531ee feat: add git-svn-rs cli surface`

Completed:

- Created Cargo workspace with `git-svn-rs` CLI crate and `git-svn-rs-core` core crate.
- Added typed CLI surface for `init`, `clone`, `fetch`, `rebase`, `dcommit`, `log`, `info`, `find-rev`, `gc`, `reset`, `diagnose`, and explicit unsupported v1 commands.
- Added CLI smoke tests and parsing tests.
- Added explicit unsupported command behavior.

Verification performed:

- `cargo metadata --no-deps`
- `cargo test --workspace`
- `cargo run -p git-svn-rs -- --help`

### Phase 2: Config, Mapping, Authors, Filters

Status: completed and committed.

Commit:

- `ab52ef1 feat: add config mapping and git metadata primitives`

Completed:

- Added SVN remote config serialization.
- Added layout mapping builders for single-path and standard-layout mappings.
- Added `GlobSpec` compatibility constraints.
- Added path and URL helpers.
- Added authors file parser.
- Added include/ignore path filters, `.git` exclusion, and ignore-over-include precedence.

Review fixes completed:

- Added missing coverage requested by review for related Phase 3 tests.
- Fixed clippy warnings that would block `-D warnings`.

Verification performed:

- `cargo fmt --check`
- `cargo test --workspace`
- `cargo clippy --all-targets --all-features -- -D warnings`

### Phase 3: Git Metadata and RevMap

Status: completed and committed.

Commit:

- `ab52ef1 feat: add config mapping and git metadata primitives`

Completed:

- Added `GitCli` wrapper and `GitBackend`-style basics.
- Added `git-svn-id` parser and formatter.
- Added `.git/svn` metadata path helper.
- Added migration metadata inspection.
- Added `.rev_map` binary read/write support for SHA-1 and SHA-256.
- Added rev_map lock file handling, `sync_all`, trailing all-zero record behavior, reset, and record listing.

Review fixes completed:

- Reject out-of-order rev_map appends.
- Lock reset read/truncate path consistently.
- Resolve `.git` gitfile and `commondir` for migration metadata discovery.
- Added missing config-not-found and process cwd mutation tests.
- Added full `MetadataOptions` conflict branch tests.

Verification performed:

- `cargo fmt --check`
- `cargo test --workspace`
- `cargo clippy --all-targets --all-features -- -D warnings`

### Phase 4/5 Foundation: SVN Abstractions, Init, Fast-Import

Status: completed and committed.

Commit:

- `44ee04d feat: add svn abstractions and init import primitives`

Completed:

- Added SVN domain types.
- Added mock SVN backend.
- Added RA session and fetch editor traits.
- Added auth prompt mock.
- Added `svn-libsvn` feature placeholder and empty gated module.
- Added fast-import stream writer.
- Implemented `git-svn-rs init`.
- Added init smoke tests.

Verification performed:

- `cargo fmt --check`
- `cargo test --workspace`
- `cargo test -p git-svn-rs-core --features svn-libsvn`
- `cargo clippy --all-targets --all-features -- -D warnings`

### Phase 4/5 Middle Layer: Fixtures, Mock Fetch, Fetch Editor

Status: completed and committed.

Commit:

- `9d84910 feat: add mock fetch import and svn fixtures`

Completed:

- Added SVN CLI fixture builder with skip-by-default behavior when SVN tools are missing.
- Added strict compatibility behavior through `GIT_SVN_RS_STRICT_COMPAT=1`.
- Added mock import planner.
- Added mock-only `clone` and `fetch` command shell.
- Added `GitCli::fast_import`.
- Added `SvnFetchEditor` behavior for file content, executable mode, symlink mode, copy, and delete planning.
- Added clone/fetch smoke tests for `mock://` URLs.

Verification performed:

- `cargo fmt --check`
- `cargo test --workspace`
- `cargo test -p git-svn-rs-core --features svn-libsvn`
- `cargo clippy --all-targets --all-features -- -D warnings`

## Implemented But Not Yet Committed

These changes are present in the working tree and still need integration, verification, review, and commit.

### Phase 6: Readonly Commands, First Batch

Status: implemented by worker, integration pending.

Implemented:

- Tracked SVN resolver for `svn-remote.svn.*` and `.git/svn/<ref>/.rev_map.<uuid>`.
- `find-rev` for `rN -> commit` and `commit -> revision`, including `--before` and `--after`.
- `GitSvnLogFormatter`.
- `log` command with `--oneline` and revision filtering.
- `info` and `info --url`.
- `gc` for stale `.rev_map.*.lock` cleanup.
- CLI wiring for `FindRev`, `Info`, `Log`, and `Gc`.
- `GitCli::rev_parse`, `GitCli::log_records`.
- `RevMap::records()`.

Known notes:

- Worker changed top-level `verbose/quiet` from global clap args to ordinary top-level args to avoid conflict with `log -v`.
- Worker reported a `log_formatter` test failure during another agent's intermediate state, then later reported `cargo test -p git-svn-rs-core` and `cargo test -p git-svn-rs` passing locally.
- Current local full workspace test timed out after compilation with a broken pipe while listing tests; needs rerun after documentation is committed or after integration cleanup.

Files currently involved:

- `crates/git-svn-rs-core/src/commands/find_rev.rs`
- `crates/git-svn-rs-core/src/commands/gc.rs`
- `crates/git-svn-rs-core/src/commands/info.rs`
- `crates/git-svn-rs-core/src/commands/log.rs`
- `crates/git-svn-rs-core/src/commands/resolver.rs`
- `crates/git-svn-rs-core/src/log_formatter.rs`
- `crates/git-svn-rs-cli/tests/readonly_commands.rs`
- `crates/git-svn-rs-core/tests/log_formatter.rs`

### Phase 7: Dcommit Core Foundations

Status: implemented by worker, integration pending.

Implemented:

- `dcommit` module shell.
- `GitDiffPlanner`.
- `SvnCommitEditor`.
- `PathEnsurer`.
- `PropertyMapper`.
- Mock commit backend adapter extensions.
- Tests for diff planner, commit editor, and mock backend commit behavior.

Known notes:

- Production `dcommit` CLI is not wired.
- Real libsvn commit editor is not implemented.
- Current API covers add, modify, delete, parent path creation, executable property mapping, and symlink property mapping.
- Rename/copy, mergeinfo, and EOL property behavior remain future work.

Files currently involved:

- `crates/git-svn-rs-core/src/dcommit/mod.rs`
- `crates/git-svn-rs-core/src/dcommit/diff_planner.rs`
- `crates/git-svn-rs-core/src/dcommit/commit_editor.rs`
- `crates/git-svn-rs-core/src/dcommit/property_mapper.rs`
- `crates/git-svn-rs-core/tests/dcommit_diff_planner.rs`
- `crates/git-svn-rs-core/tests/dcommit_commit_editor.rs`
- `crates/git-svn-rs-core/src/svn/mock.rs`
- `crates/git-svn-rs-core/src/svn/types.rs`
- `crates/git-svn-rs-core/tests/svn_backend_mock.rs`

### Phase 7: Shim and Windows Verification

Status: implemented by worker, integration pending.

Implemented:

- Added `git-svn` shim crate.
- Added shim forwarding behavior to `git-svn-rs`.
- Added shim smoke tests for `--help` and `diagnose`.
- Added `scripts/verify.ps1`.
- Added Windows GitHub Actions workflow.
- Added README verification/dependency notes.

Known notes:

- Shim is a thin forwarding layer only.
- Full `scripts/verify.ps1` was blocked during worker validation by unrelated intermediate formatting changes; full verification should be rerun after integration cleanup.

Files currently involved:

- `crates/git-svn-rs-shim/Cargo.toml`
- `crates/git-svn-rs-shim/src/main.rs`
- `crates/git-svn-rs-cli/tests/shim.rs`
- `scripts/verify.ps1`
- `.github/workflows/windows.yml`
- `README.md`
- root `Cargo.toml`
- `Cargo.lock`

## Not Completed Yet

### Phase 4 Remaining

- Real `svn-libsvn` backend implementation.
- Deeper auth/libsvn integration.

### Phase 5 Remaining

- Complete SVN replay-backed `fetch` beyond the current minimal local `file://` SVN CLI fixture path.
- Complete replay-backed `clone` beyond the current minimal local `file://` SVN CLI fixture path.
- Full integration between RA session, `SvnFetchEditor`, fast-import, metadata, and rev_map for branches/tags and remote SVN servers.
- Branch/tag discovery and copy-from behavior against real SVN history.
- Full compatibility handling for empty dirs, absent paths, executable bits, symlinks, and `git-svn-id` footers in real fetch output.

Current limitation:

- `clone` and `fetch` support `mock://` plus a minimal local `file://` SVN CLI fixture path.
- Non-file remote SVN URLs still return a clear unsupported error.

### Phase 6 Remaining

- Full integration of `log` output with all planned `Log.pm` compatibility modes.
- Broader resolver behavior for multi-ref layouts and more complex rev_map discovery.

### Phase 7 Remaining

- Production `dcommit` write-back wiring.
- Linear write-back acceptance test.
- Real editor-driven write-back through libsvn or equivalent backend.
- Fetch new SVN revision after dcommit.
- Optional rebase after dcommit unless `--no-rebase`.

Current limitation:

- Non-dry-run `dcommit` is implemented only for `mock://` tracked configurations.
- Real SVN/libsvn write-back is still guarded.

### Phase 8 Remaining

- Rust-vs-Perl artifact comparison.
- Edge case golden scenarios.
- Strict compatibility verification mode in CI.
- Golden comparisons for config, refs, log output, git-svn-id footers, rev_map records, file modes, symlinks, copies, deletes, and command output.

## Current Verification Snapshot

Last committed baseline (`9d84910`) was verified with:

- `cargo fmt --check`
- `cargo test --workspace`
- `cargo test -p git-svn-rs-core --features svn-libsvn`
- `cargo clippy --all-targets --all-features -- -D warnings`

Current working tree after latest integration:

- `cargo test -p git-svn-rs --test clone_fetch_real_svn clone_file_url_applies_ignore_paths_filter -- --nocapture` passed.
- `cargo test -p git-svn-rs --test clone_fetch_real_svn clone_file_url_applies_include_paths_filter -- --nocapture` passed.
- `cargo test -p git-svn-rs --test clone_fetch_real_svn -- --nocapture` passed with 7 tests.
- `cargo test -p git-svn-rs --test dcommit_linear dcommit_removes_special_property_from_file_svn_when_link_becomes_file -- --nocapture` passed.
- `cargo test -p git-svn-rs --test dcommit_linear -- --nocapture` passed with 12 tests.
- `cargo test -p git-svn-rs --test dcommit_linear dcommit_removes_executable_property_from_file_svn_when_mode_is_cleared -- --nocapture` passed.
- `cargo test -p git-svn-rs --test dcommit_linear -- --nocapture` passed with 11 tests.
- `cargo test -p git-svn-rs-core --test compat_golden artifact_comparison_reports_supported_subset_mismatches` passed.
- `cargo test -p git-svn-rs-core --test compat_golden -- --nocapture` passed, with Perl git-svn comparison skipped when Perl git-svn is unavailable.
- `cargo test -p git-svn-rs --test clone_fetch_real_svn clone_stdlayout_file_url_preserves_empty_dirs_when_requested -- --nocapture` passed.
- `cargo test -p git-svn-rs --test clone_fetch_real_svn -- --nocapture` passed with 5 tests.
- `cargo fmt --check` passed.
- `cargo clippy --workspace --all-targets -- -D warnings` passed.
- `cargo test --workspace` passed.
- `cargo test -p git-svn-rs --test dcommit_linear dcommit_writes_special_property_to_file_svn_when_tools_exist -- --nocapture` passed.
- `cargo test -p git-svn-rs --test dcommit_linear -- --nocapture` passed with 10 tests.
- `cargo test -p git-svn-rs --test dcommit_linear dcommit_writes_executable_property_to_file_svn_when_tools_exist -- --nocapture` passed.
- `cargo test -p git-svn-rs --test dcommit_linear -- --nocapture` passed with 9 tests.
- `cargo test -p git-svn-rs --test dcommit_linear dcommit_writes_file_adds_and_deletes_to_file_svn_when_tools_exist -- --nocapture` passed.
- `cargo test -p git-svn-rs --test dcommit_linear -- --nocapture` passed with 8 tests.
- `cargo test -p git-svn-rs --test dcommit_linear dcommit_rebases_after_file_svn_write_by_default -- --nocapture` passed.
- `cargo test -p git-svn-rs --test dcommit_linear -- --nocapture` passed with 7 tests.
- `cargo test -p git-svn-rs --test dcommit_linear dcommit_writes_linear_commit_to_file_svn_when_tools_exist -- --nocapture` passed.
- `cargo test -p git-svn-rs --test dcommit_linear -- --nocapture` passed.
- `cargo test -p git-svn-rs-core --test fast_import` passed.
- `cargo test -p git-svn-rs-core --test import_mock` passed.
- `cargo test -p git-svn-rs --test clone_fetch_real_svn -- --nocapture` passed.
- `cargo clippy --workspace --all-targets -- -D warnings` passed.
- `cargo test --workspace` passed.
- `cargo test -p git-svn-rs --test dcommit_linear dcommit_mergeinfo_reports_v1_scope_message` passed.
- `cargo test -p git-svn-rs --test dcommit_linear` passed.
- `cargo fmt --check` passed.
- `cargo test -p git-svn-rs --test readonly_commands info_url` passed.
- `cargo test -p git-svn-rs-core --test compat_golden -- --nocapture` passed, with the Perl git-svn comparison skipped when Perl git-svn is unavailable.
- `cargo test --workspace` passed.
- `cargo test -p git-svn-rs-core --features svn-libsvn` passed.
- `cargo clippy --all-targets --all-features -- -D warnings` passed.
- `powershell -ExecutionPolicy Bypass -File scripts\verify.ps1` passed.

## Recommended Next Steps

1. Commit the integrated Phase 6/7/8 skeleton work.
2. Continue with:
   - real production `dcommit` through libsvn or equivalent backend
   - complete SVN replay fetch/clone for branch/tag/copy/empty-dir edge cases
   - real Rust-vs-Perl artifact comparison
   - real `svn-libsvn` backend wiring
