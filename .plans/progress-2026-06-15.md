# git-svn-rs Implementation Progress - 2026-06-15

This document records the current execution state of the plans in `.plans/`.

## Current Branch

- Branch: `codex-execute-git-svn-rs-plans`
- Base: `master` at `1284668 Add planning documents`

## Latest Update

New performed this turn for `svn-libsvn`:

- Rechecked local feasibility without network.
- Found TortoiseSVN `svn.exe` 1.14.0 and runtime DLLs only; no `pkg-config`, SVN headers, import libraries, or SVN-related env vars were available.
- Found cached Rust crate `svn` 0.1.8, but it is a pure RA/SVN protocol crate rather than libsvn FFI.
- Kept the feature as a compile-only shell: default builds still do not require libsvn, feature builds report enabled/not-linked, and `LibSvnBackend` methods return a clear not-linked error.

The Phase 6/7/8 follow-up work has been integrated locally and verified. It is pending commit.

Newly completed locally:

- Phase 6 `reset`
- Phase 6 `rebase`
- Phase 7 `dcommit --dry-run`
- Phase 7 `--mergeinfo` v1 scope message
- Phase 8 golden compatibility harness skeleton
- Phase 4 diagnostics/libsvn feature shell
- Phase 5 minimal real `file://` SVN CLI fetch/clone replay
- Phase 7 mock-only `dcommit` write-back

Latest verification:

- `cargo fmt --check`
- `cargo test --workspace`
- `cargo test -p git-svn-rs-core --features svn-libsvn`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `powershell -ExecutionPolicy Bypass -File scripts\verify.ps1`
- `cargo test -p git-svn-rs --test clone_fetch_real_svn`
- `cargo test -p git-svn-rs --test dcommit_linear`
- `cargo test -p git-svn-rs --features svn-libsvn diagnose_prints_enabled_when_libsvn_feature_is_compiled`

Note: one Windows cargo run emitted a non-fatal incremental compilation cleanup warning under `target/`; all commands exited successfully.

## Committed Completed Work

### Phase 1: Foundation CLI Workspace

Status: completed and committed.

Commit:

- `7f531ee feat: add git-svn-rs cli surface`

Completed:

- Created Cargo workspace with `git-svn-rs` CLI crate and `git-svn-rs-core` core crate.
- Added typed CLI surface for `init`, `clone`, `fetch`, `rebase`, `dcommit`, `log`, `info`, `find-rev`, `gc`, `reset`, `diagnose`, and explicit unsupported v1 commands.
- Added CLI smoke tests and parsing tests.
- Added explicit unsupported command behavior.

Verification performed:

- `cargo metadata --no-deps`
- `cargo test --workspace`
- `cargo run -p git-svn-rs -- --help`

### Phase 2: Config, Mapping, Authors, Filters

Status: completed and committed.

Commit:

- `ab52ef1 feat: add config mapping and git metadata primitives`

Completed:

- Added SVN remote config serialization.
- Added layout mapping builders for single-path and standard-layout mappings.
- Added `GlobSpec` compatibility constraints.
- Added path and URL helpers.
- Added authors file parser.
- Added include/ignore path filters, `.git` exclusion, and ignore-over-include precedence.

Review fixes completed:

- Added missing coverage requested by review for related Phase 3 tests.
- Fixed clippy warnings that would block `-D warnings`.

Verification performed:

- `cargo fmt --check`
- `cargo test --workspace`
- `cargo clippy --all-targets --all-features -- -D warnings`

### Phase 3: Git Metadata and RevMap

Status: completed and committed.

Commit:

- `ab52ef1 feat: add config mapping and git metadata primitives`

Completed:

- Added `GitCli` wrapper and `GitBackend`-style basics.
- Added `git-svn-id` parser and formatter.
- Added `.git/svn` metadata path helper.
- Added migration metadata inspection.
- Added `.rev_map` binary read/write support for SHA-1 and SHA-256.
- Added rev_map lock file handling, `sync_all`, trailing all-zero record behavior, reset, and record listing.

Review fixes completed:

- Reject out-of-order rev_map appends.
- Lock reset read/truncate path consistently.
- Resolve `.git` gitfile and `commondir` for migration metadata discovery.
- Added missing config-not-found and process cwd mutation tests.
- Added full `MetadataOptions` conflict branch tests.

Verification performed:

- `cargo fmt --check`
- `cargo test --workspace`
- `cargo clippy --all-targets --all-features -- -D warnings`

### Phase 4/5 Foundation: SVN Abstractions, Init, Fast-Import

Status: completed and committed.

Commit:

- `44ee04d feat: add svn abstractions and init import primitives`

Completed:

- Added SVN domain types.
- Added mock SVN backend.
- Added RA session and fetch editor traits.
- Added auth prompt mock.
- Added `svn-libsvn` feature placeholder and empty gated module.
- Added fast-import stream writer.
- Implemented `git-svn-rs init`.
- Added init smoke tests.

Verification performed:

- `cargo fmt --check`
- `cargo test --workspace`
- `cargo test -p git-svn-rs-core --features svn-libsvn`
- `cargo clippy --all-targets --all-features -- -D warnings`

### Phase 4/5 Middle Layer: Fixtures, Mock Fetch, Fetch Editor

Status: completed and committed.

Commit:

- `9d84910 feat: add mock fetch import and svn fixtures`

Completed:

- Added SVN CLI fixture builder with skip-by-default behavior when SVN tools are missing.
- Added strict compatibility behavior through `GIT_SVN_RS_STRICT_COMPAT=1`.
- Added mock import planner.
- Added mock-only `clone` and `fetch` command shell.
- Added `GitCli::fast_import`.
- Added `SvnFetchEditor` behavior for file content, executable mode, symlink mode, copy, and delete planning.
- Added clone/fetch smoke tests for `mock://` URLs.

Verification performed:

- `cargo fmt --check`
- `cargo test --workspace`
- `cargo test -p git-svn-rs-core --features svn-libsvn`
- `cargo clippy --all-targets --all-features -- -D warnings`

## Implemented But Not Yet Committed

These changes are present in the working tree and still need integration, verification, review, and commit.

### Phase 6: Readonly Commands, First Batch

Status: implemented by worker, integration pending.

Implemented:

- Tracked SVN resolver for `svn-remote.svn.*` and `.git/svn/<ref>/.rev_map.<uuid>`.
- `find-rev` for `rN -> commit` and `commit -> revision`, including `--before` and `--after`.
- `GitSvnLogFormatter`.
- `log` command with `--oneline` and revision filtering.
- `info` and `info --url`.
- `gc` for stale `.rev_map.*.lock` cleanup.
- CLI wiring for `FindRev`, `Info`, `Log`, and `Gc`.
- `GitCli::rev_parse`, `GitCli::log_records`.
- `RevMap::records()`.

Known notes:

- Worker changed top-level `verbose/quiet` from global clap args to ordinary top-level args to avoid conflict with `log -v`.
- Worker reported a `log_formatter` test failure during another agent's intermediate state, then later reported `cargo test -p git-svn-rs-core` and `cargo test -p git-svn-rs` passing locally.
- Current local full workspace test timed out after compilation with a broken pipe while listing tests; needs rerun after documentation is committed or after integration cleanup.

Files currently involved:

- `crates/git-svn-rs-core/src/commands/find_rev.rs`
- `crates/git-svn-rs-core/src/commands/gc.rs`
- `crates/git-svn-rs-core/src/commands/info.rs`
- `crates/git-svn-rs-core/src/commands/log.rs`
- `crates/git-svn-rs-core/src/commands/resolver.rs`
- `crates/git-svn-rs-core/src/log_formatter.rs`
- `crates/git-svn-rs-cli/tests/readonly_commands.rs`
- `crates/git-svn-rs-core/tests/log_formatter.rs`

### Phase 7: Dcommit Core Foundations

Status: implemented by worker, integration pending.

Implemented:

- `dcommit` module shell.
- `GitDiffPlanner`.
- `SvnCommitEditor`.
- `PathEnsurer`.
- `PropertyMapper`.
- Mock commit backend adapter extensions.
- Tests for diff planner, commit editor, and mock backend commit behavior.

Known notes:

- Production `dcommit` CLI is not wired.
- Real libsvn commit editor is not implemented.
- Current API covers add, modify, delete, parent path creation, executable property mapping, and symlink property mapping.
- Rename/copy, mergeinfo, and EOL property behavior remain future work.

Files currently involved:

- `crates/git-svn-rs-core/src/dcommit/mod.rs`
- `crates/git-svn-rs-core/src/dcommit/diff_planner.rs`
- `crates/git-svn-rs-core/src/dcommit/commit_editor.rs`
- `crates/git-svn-rs-core/src/dcommit/property_mapper.rs`
- `crates/git-svn-rs-core/tests/dcommit_diff_planner.rs`
- `crates/git-svn-rs-core/tests/dcommit_commit_editor.rs`
- `crates/git-svn-rs-core/src/svn/mock.rs`
- `crates/git-svn-rs-core/src/svn/types.rs`
- `crates/git-svn-rs-core/tests/svn_backend_mock.rs`

### Phase 7: Shim and Windows Verification

Status: implemented by worker, integration pending.

Implemented:

- Added `git-svn` shim crate.
- Added shim forwarding behavior to `git-svn-rs`.
- Added shim smoke tests for `--help` and `diagnose`.
- Added `scripts/verify.ps1`.
- Added Windows GitHub Actions workflow.
- Added README verification/dependency notes.

Known notes:

- Shim is a thin forwarding layer only.
- Full `scripts/verify.ps1` was blocked during worker validation by unrelated intermediate formatting changes; full verification should be rerun after integration cleanup.

Files currently involved:

- `crates/git-svn-rs-shim/Cargo.toml`
- `crates/git-svn-rs-shim/src/main.rs`
- `crates/git-svn-rs-cli/tests/shim.rs`
- `scripts/verify.ps1`
- `.github/workflows/windows.yml`
- `README.md`
- root `Cargo.toml`
- `Cargo.lock`

## Not Completed Yet

### Phase 4 Remaining

- Real `svn-libsvn` backend implementation.
- Deeper auth/libsvn integration.

### Phase 5 Remaining

- Complete SVN replay-backed `fetch` beyond the current minimal local `file://` SVN CLI fixture path.
- Complete replay-backed `clone` beyond the current minimal local `file://` SVN CLI fixture path.
- Full integration between RA session, `SvnFetchEditor`, fast-import, metadata, and rev_map for branches/tags and remote SVN servers.
- Branch/tag discovery and copy-from behavior against real SVN history.
- Full compatibility handling for empty dirs, absent paths, executable bits, symlinks, and `git-svn-id` footers in real fetch output.

Current limitation:

- `clone` and `fetch` support `mock://` plus a minimal local `file://` SVN CLI fixture path.
- Non-file remote SVN URLs still return a clear unsupported error.

### Phase 6 Remaining

- Full integration of `log` output with all planned `Log.pm` compatibility modes.
- Broader resolver behavior for multi-ref layouts and more complex rev_map discovery.

### Phase 7 Remaining

- Production `dcommit` write-back wiring.
- Linear write-back acceptance test.
- Real editor-driven write-back through libsvn or equivalent backend.
- Fetch new SVN revision after dcommit.
- Optional rebase after dcommit unless `--no-rebase`.

Current limitation:

- Non-dry-run `dcommit` is implemented only for `mock://` tracked configurations.
- Real SVN/libsvn write-back is still guarded.

### Phase 8 Remaining

- Rust-vs-Perl artifact comparison.
- Edge case golden scenarios.
- Strict compatibility verification mode in CI.
- Golden comparisons for config, refs, log output, git-svn-id footers, rev_map records, file modes, symlinks, copies, deletes, and command output.

## Current Verification Snapshot

Last committed baseline (`9d84910`) was verified with:

- `cargo fmt --check`
- `cargo test --workspace`
- `cargo test -p git-svn-rs-core --features svn-libsvn`
- `cargo clippy --all-targets --all-features -- -D warnings`

Current working tree after latest integration:

- `cargo fmt --check` passed.
- `cargo test --workspace` passed.
- `cargo test -p git-svn-rs-core --features svn-libsvn` passed.
- `cargo clippy --all-targets --all-features -- -D warnings` passed.
- `powershell -ExecutionPolicy Bypass -File scripts\verify.ps1` passed.

## Recommended Next Steps

1. Commit the integrated Phase 6/7/8 skeleton work.
2. Continue with:
   - real production `dcommit` through libsvn or equivalent backend
   - complete SVN replay fetch/clone for branch/tag/copy/empty-dir edge cases
   - real Rust-vs-Perl artifact comparison
   - real `svn-libsvn` backend wiring
